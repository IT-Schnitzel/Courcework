server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auditorium_db
    username: root
    password: 1
    driver-class-name: org.postgresql.Driver
jpa:
  hibernate:
    ddl-auto: update
  show-sql: true

spring.jpa.show-sql: true
spring.jpa.properties.hibernate.format_sql: true
